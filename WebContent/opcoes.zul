<?init class="br.ufjf.avaliacao.controller.AuthController"?>
<window  xmlns:w="client" width="100%" title="Respostas" border="normal"
	apply="org.zkoss.bind.BindComposer" closable="false"
	viewModel="@id('vm') @init('br.ufjf.avaliacao.controller.QuestionariosController')">
	

	<grid model="@load(vm.lista_opcoes)">
		<columns></columns>
		<template name="model">
			<row>
				<textbox width="100%" value="@load(each) @save(each, before='confirm')" onChange="@command('salvarMudancaOpcao',opcao=each,nova_opcao=self.value,url=url.value)"/>			
			</row>
		</template>
		
	</grid>
	
	
</window>